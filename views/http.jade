h3 Http Services
table.table.table-bordered.table-condensed
    col(style='width: 7em;')
    col(style='width: 20em;')
    col(style='width: 4em;')
    col
    col(style='width: 8em;')
    thead
        tr(style='background-color: Black;')
            th(style='color: WhiteSmoke; text-align:center;') Method
            th(style='color: WhiteSmoke; text-align:center;') URL
            th(style='color: WhiteSmoke; text-align:center;') Status
            th(style='color: WhiteSmoke; text-align:center;') Response
            th(style='color: WhiteSmoke; text-align:center;') Content Type
    tbody
        each service in services['http']
            if(service.method == 'GET')
                tr.success
                    include http_table_cols
            else if(service.method == 'POST')
                tr.info
                    include http_table_cols
            else if(service.method == 'PUT')
                tr.warning
                    include http_table_cols
            else if(service.method == 'DELETE')
                tr.error
                    include http_table_cols